# Build and Release Folders
bin-debug/
bin-release/
[Oo]bj/
[Bb]in/

# Other files and folders
.settings/

# Executables
*.swf
*.air
*.ipa
*.apk

# Project files, i.e. `.project`, `.actionScriptProperties` and `.flexProperties`
# should NOT be excluded as they contain compiler settings and other important
# information for Eclipse / Flash Builder.
# Test

# FTP credentials (contains sensitive login info)
photoscavenger/ftp-config.json

# Local-only billing kill-switch ops cheat sheet (IDs + adjust commands)
photoscavenger/BILLING-KILL-SWITCH.local.md

# Firebase CLI local cache/state
.firebase/

# Couplegames build output (built in CI)
couplegames/dist/
couplegames/node_modules/
couplegames/.env.local